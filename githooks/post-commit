#!/bin/sh
# post-commit — 编长器commit后自动通知内核版本快照
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
REPO_ROOT="$(cd "${SCRIPT_DIR}/.." && pwd)"
SNAPSHOT_SCRIPT="/vol1/@team/qh团队/QH/AI专用/Agent OS/kernel/src/purpose_manager.py"

if [ -f "$SNAPSHOT_SCRIPT" ]; then
    python3 "$SNAPSHOT_SCRIPT" snapshot --trigger "editor_update"
fi
