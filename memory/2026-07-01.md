# 2026-07-01 工作日志

## 核心变更：workspace/memory/ 软链 → 真目录

### 问题
`workspace/memory/` 是软链指向 `/轻如烟/memory/`，OpenClaw 的 `write`/`read` 工具内置 fs-safe 检查拒绝跨 workspace 根的 symlink → 自动内存刷写（pre-compaction flush）写入失败。

### 解决
- 删软链：`rm workspace/memory`
- 建真目录：`mkdir workspace/memory`
- 复制现有 44 个记忆文件过来
- 加系统 crontab 每分钟双向 rsync：
  - `workspace/memory/` ↔ `/轻如烟/memory/`

### 其他进展
- ✅ 提醒系统 toggle 逻辑修复完成（子代理 `v5-bug-reminder-toggle`）
  - `secretary.py`: 不再过滤 done
  - `helper_handler.py`: done → toggle
  - `render.js`: ✅ + 删除线
  - 21/21 测试通过
- ✅ v5.0 备份同步：69 文件，11 JS 全部 acorn 验证通过
- ✅ 子代理 `audit-config-usage` 产出 707 行审计报告
- ⏳ 待处理：Control UI 三开关全开（安全风险）
- ⏳ 待处理：DeepSeek `input: ["image"]` 改为 `["text"]`
- ⏳ 待处理：Compaction 默认值调优
