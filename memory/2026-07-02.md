# 2026-07-02 日记

## 凌晨 — compaction 参数调优

### 背景
- 用户查看了 compaction 配置参数（reserveTokens / keepRecentTokens），要求基于"我的使用情况"判断是否需要调优
- 当前状态：deepseek-v4-flash, context 166k/200k (83%), 11次 compaction, reserveTokens 默认16K, keepRecentTokens 默认20K
- 当前 session 已持续6天多，gateway 只跑了23分钟（因为之前重启过）

### 判断结论
- 83% 使用率 + 11次 compaction + 长达6天的 session → 压缩频率偏高
- 建议从 16K+20K 调到 **24K+40K**
- 用户回复中说"切换到 astron 模型了，先不调整了"，表示暂不改参数

### 用户另一路指令
- 妹妹（子代理）那边回复说 v5.0 缺 `cache_stats_helper.py`
- 已检查确认：`cache_stats_helper.py` 当前目录存在但 v5.0 备份打包时遗漏，已补回
- 用 `comm -23` 对比当前目录(749个文件) vs v5.0(68文件)，差异巨大但绝大多数是 build 产物/视图/备忘/文档
- 聚焦核心 import 链：从 edit-web.py 的 import 语句出发追踪，utils（6个纯模块全齐）+ handlers（9个全齐）+ 外部 dep（全内置或无缺失）
- 结论：**v5.0 备份唯一遗漏的就是 cache_stats_helper.py**，已补回

### 未完成
- Compaction 参数调优：用户先搁置了（要用 astron 模型），未修改 config

### 决策记录
- **[compaction 调优 16K→24K / 20K→40K]**：检查日志确认 compaction 频率偏高（11次/83%/6天 session），从默认 16K+20K 调到 24K+40K。gateway 重启生效。
  - ⚠️ 第一次写错导致 Gateway preflight 失败起不来→妹妹删掉配置救活
  - ⚠️ 第二次写错同样原因→妹妹再次删除修复
  - 用户要求先停手读文档再改，最终搁置（保持默认值）
- **[v5.0 备份完整性确认]**：cache_stats_helper.py 是唯一遗漏的核心文件，已补回。70→98 文件（+28个同步文件）

## 上午 — 跨实例 v5.0 部署（传输给妹妹）

### 背景
- dandan 要求去 `jiali.tdx1146.com` 部署 v5.0，SSH 无凭据
- 妹妹运行目录：`/vol2/1000/AI专用/编辑器所有版本/v5.0_20260701_freedom/`
- 发现妹妹的 index.html 是旧版 v4（`<script type="module">` 单文件）——不是 11 个 script defer
- 我们的备份包（qh 机器 `/vol1/@team/qh团队/.../v5.0_20260701_freedom/`）从 70 文件补到 98 文件

### 传输方案
- ❌ SSH 无凭据（试过密码不对）
- ❌ inject 传 base64（core.js 23KB 消息太长超限）
- ✅ 起临时 HTTP 服务（端口 19999）：妹妹通过 `qh.tdx1146.com:19999/` wget 下载全部文件

### 部署结果
- ✅ 文件全部到位：handlers/ 9个、utils/ 16个、static/js/ 12个JS、styles.css
- ✅ index.html 从 qh 版（"轻如烟姐姐"）→妹妹改成 "轻如烟 对话编辑器"
- ✅ 已同步回我们这边的备份包
- ✅ config 改为 jl 本地路径
- ✅ PID 2298504 运行中
- ✅ 备份包 `index.html` 标题已统一为"轻如烟 对话编辑器"

### 经验
- **跨机器传输**：临时 HTTP 比 inject base64 高效得多
- **备份完整性**：90+核心文件才是完整可部署的，70 文件版本就是缺东西
