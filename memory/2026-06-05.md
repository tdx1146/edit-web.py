# 2026-06-05 (11:28 AM CST)

## jl (jiali/轻如烟妹) edit-web.py 崩溃修复

### 根因链（三层同一根）
- **启动环境不完整**：SSH 拉起 edit-web.py 时没有设置 `OPENCLAW_HOME` 和 `GATEWAY_PORT` 环境变量
  - 第一层：`find_openclaw_home()` 回退到 `~/.openclaw` → `/home/trim.openclaw/.openclaw`（路径存在但配置旧，Gateway 端口 22881 而非 24020）
  - 第二层：`GATEWAY_PORT` 默认 22881 → inject-helper.mjs 连错 Gateway，`IDENTITY_PATH` 指向错误路径
  - 第三层：`node` 不在 nohup 的 PATH 中 → `subprocess.Popen(["node", ...])` 抛 `FileNotFoundError`
    - jl 系统没装 nodejs，但有 bun (`/var/apps/bunjs/target/bin/bun`)

### 修复操作
1. **改 inject_via_websocket** (jl edit-web.py 第207-208行)：
   - 在 `env = os.environ.copy()` 后加了 `env['PATH'] = '/usr/bin:/usr/local/bin:/bin:/usr/sbin'`
   - 替换 `"node", helper` 为 `"/var/apps/bunjs/target/bin/bun", helper`
2. **启动时必须带 env vars**（问题仍会复现——需写启动脚本固化）：
   ```
   export OPENCLAW_HOME=/vol1/@apphome/trim.openclaw/data/home/.openclaw
   export GATEWAY_PORT=24020
   cd /vol2/1000/AI专用/所有自动化/轻如烟/scripts && nohup python3 edit-web.py &
   ```
3. qh 侧 edit-web.py 18888 被 kill 后端口锁住（TIME_WAIT），尚未重启

### 修复后状态
- jl inject 返回 `{"ok": true}` —— 通了
- jl 18888 口锁了，改用 18887 测试通过
- jl /quickcheck 返回正常
- qh 18888 端口被 kill 后锁住，编辑器和 inject 均不可用

## 六并三协议确认

### 三条方案（姐妹踱步碰撞结果）
1. **META 标签做三系统公共语言**（不新增 MAP 层级）—— 已在 facts.dict.md 内，读写不依赖服务健康
2. **即时验证**：`/quickcheck` 随时自检 + 创建后 cron run/curl/exec 立即触发
   - qh 侧已加 `/api/quickcheck` handler（do_POST elif + _handle_quickcheck 方法）
   - jl 侧已由姐姐加好
3. **digest step0**：读 system-delta → facts.dict → backlog，事件→归档→断言→反馈，强制同标签引用
   - 已跑在 cron's isolated session 里，不受 Gateway 状态影响

### 自活共识
三条都不依赖任何中间件：META 存文件、quickcheck 跑 Python 不调 node/bun、digest 跑 cron。  
fallback 机制暂不需要——等出现故障再补。

## UI 修改
- qh index.html: `轻如烟` → `轻如烟姐`（title + h1），方便区分姐妹实例
- jl 侧建议改 `轻如烟妹`，待执行

## 基础设施状态
- qh Gateway: 活，但 edit-web.py 18888 端口锁住
- jl Gateway: 活（PID 1303573, Jun 3 启动），edit-web.py 已修好
- jl 端口情况: 18888 TIME_WAIT → 18887 替代使用
- qh edit-web.py: python3, bun 1.3.9, Node.js 18.20.4
- jl edit-web.py: python3, bun (`/var/apps/bunjs/target/bin/bun`), 无 nodejs

## 关键路径
- edit-web.py 执行 inject 的 PATH: `os.path.dirname(__file__)/inject-helper.mjs` → 通过 subprocess 调用 node/bun
- jl edit-web.py 路径: `/vol2/1000/AI专用/所有自动化/轻如烟/scripts/edit-web.py`
- jl Gateway 路径: `/vol1/@appcenter/trim.openclaw/server/index.js` (bun)
