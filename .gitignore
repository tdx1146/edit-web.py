# ═══ 密钥与实例配置（绝不分发）═══
api_keys.py            # 已无硬编码密钥(纯env读取)，但保持不入库双保险
.env
.env.*
!*.example
openclaw.json*
identity/
system-config/

# ═══ 运行时数据（绝不分发）═══
.inject_logs/
.locks/
.踱步/
sessions/
*.trajectory.jsonl
*.jsonl.reset.*
*.checkpoint.*
backup/
revisions/
experimental/
deploy-notes/
logs/
releases/              # 打包产物输出目录
*.tar.gz

# ═══ 噪音/备份（绝不分发）═══
__pycache__/
*.pyc
*.log
*.bak*
*.lock

# ═══ 他人仓库副本 ═══
sandglass_source/
edit-web-github/

# ═══ 备份类文件补充规则 ═══
*.backup.*
*.old.*
*.covered-*
*.bak.*
